# 🔐 Система авторизации МойДосуг

## Реализованные методы входа

### 1. **Email + пароль** 📧
- Регистрация с именем, email и паролем (минимум 6 символов)
- Стандартный вход для зарегистрированных пользователей
- Валидация email формата

### 2. **Номер телефона** 📱
- Вход/регистрация по номеру телефона
- SMS-код из 4 цифр для подтверждения
- Автоматическое форматирование номера
- Возможность повторной отправки кода

### 3. **Google аккаунт** 🔑
- Быстрая авторизация через Google
- Автоматическое получение имени и email
- Один клик для входа

## 🎨 Функционал

### Защита действий
Неавторизованные пользователи **не могут**:
- ❌ Создавать объявления
- ❌ Ставить лайки/дизлайки
- ❌ Добавлять в избранное
- ❌ Оставлять комментарии
- ❌ Отправлять сообщения

При попытке этих действий появляется:
- Уведомление о необходимости авторизации
- Автоматическое открытие окна входа

### Профиль пользователя
После входа доступно:
- ✅ Просмотр информации профиля
- ✅ ID пользователя
- ✅ Дата регистрации
- ✅ Метод входа (badge с иконкой)
- ✅ Выход из аккаунта

### Хранение данных
- **localStorage** для хранения сессии пользователя
- Автоматическое восстановление сессии при перезагрузке
- Данные не теряются при закрытии браузера

## 📱 UX/UI особенности

### Адаптивный дизайн
- ✅ Полностью адаптивные диалоги
- ✅ Touch-friendly кнопки
- ✅ Автофокус на полях ввода
- ✅ Валидация в реальном времени

### Визуальная обратная связь
- **Тосты** для всех действий (успех/ошибка)
- **Иконки** для каждого метода входа
- **Бейджи** для отображения метода авторизации
- **Аватары** с первой буквой имени

### Безопасность
- Пароли скрыты (type="password")
- Минимальная длина пароля (6 символов)
- Подтверждение пароля при регистрации
- Валидация email формата
- Проверка номера телефона (минимум 10 цифр)

## 🔄 Переключение между методами

В диалоге авторизации можно:
1. **Email ⇄ Телефон** - кнопки переключения внизу
2. **Вход ⇄ Регистрация** - ссылки внизу формы
3. **Google** - всегда доступен сверху

## 📊 Технические детали

### Структура пользователя
```typescript
{
  id: number,           // Уникальный ID
  name: string,         // Имя пользователя
  email?: string,       // Email (для email/google)
  phone?: string,       // Телефон (для phone)
  authMethod: string,   // 'email' | 'phone' | 'google'
  registeredAt: string  // ISO дата регистрации
}
```

### Компоненты
- **AuthDialog.tsx** - модальное окно входа/регистрации
- **UserProfile.tsx** - профиль пользователя в шапке

### Интеграция с приложением
- Проверка авторизации в Index.tsx
- Автоматическое открытие AuthDialog при защищённых действиях
- Сохранение currentUser в state

## 🚀 Будущие улучшения

### Планируется добавить:
- [ ] Реальная интеграция с Google OAuth
- [ ] Реальная отправка SMS через провайдера
- [ ] Восстановление пароля
- [ ] Подтверждение email
- [ ] Двухфакторная аутентификация
- [ ] Социальные сети (VK, Telegram)
- [ ] Редактирование профиля
- [ ] Смена пароля
- [ ] Удаление аккаунта

## 💡 Использование

### Для разработчика
```typescript
// Проверить авторизацию
if (!currentUser) {
  setShowAuthDialog(true);
  return;
}

// Получить данные пользователя
const userData = JSON.parse(localStorage.getItem('currentUser'));

// Выход
localStorage.removeItem('currentUser');
setCurrentUser(null);
```

### Для пользователя
1. Нажмите **"Войти"** в шапке
2. Выберите удобный метод входа
3. Заполните данные
4. Готово! Можете пользоваться всеми функциями

---

**Примечание:** Текущая реализация использует мок-данные для демонстрации. Для production необходима интеграция с реальным бэкендом.
